"""Models. Pure stdlib by design -- no numpy, scipy, scikit-learn, or pandas.

With ~800 FBS games a season, regularised linear models, Elo, and isotonic
calibration beat gradient boosting on variance grounds, so the dependency
constraint and the statistically correct choice coincide.
"""
